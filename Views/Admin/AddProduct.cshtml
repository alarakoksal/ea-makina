@model ea_makina.Models.Product

@{
    Layout = "_AdminLayout";
}

<style>
    @@import url('https://fonts.googleapis.com/css2?family=Syncopate:wght@400;700&family=Inter:wght@300;600;900&display=swap');

    :root {
        --accent: #f59e0b;
        --dark-bg: #0a0a0a;
        --input-bg: rgba(255, 255, 255, 0.03);
    }

    body { background: #fdfdfd; font-family: 'Inter', sans-serif; }

    .admin-form-wrapper {
        max-width: 800px;
        margin: 60px auto;
    }

    .premium-form-card {
        background: var(--dark-bg);
        border-radius: 30px;
        padding: 50px;
        border: 1px solid rgba(245, 158, 11, 0.2); 
        box-shadow: 0 40px 80px rgba(0,0,0,0.15);
        position: relative;
    }

    .form-header h2 {
        font-family: 'Syncopate', sans-serif;
        color: #fff;
        font-size: 1.4rem;
        letter-spacing: 6px;
        text-transform: uppercase;
        margin-bottom: 40px;
        border-left: 4px solid var(--accent);
        padding-left: 20px;
    }

    .form-label {
        color: var(--accent);
        font-weight: 800;
        font-size: 0.7rem;
        letter-spacing: 2px;
        text-transform: uppercase;
        margin-bottom: 10px;
        display: block;
    }

    .form-control-premium {
        background: var(--input-bg) !important;
        border: 1px solid rgba(255, 255, 255, 0.1) !important;
        border-radius: 12px !important;
        padding: 15px 20px !important;
        color: #fff !important;
        transition: 0.3s !important;
    }

    .form-control-premium:focus {
        border-color: var(--accent) !important;
        box-shadow: 0 0 15px rgba(245, 158, 11, 0.1) !important;
    }

    
    .btn-group-custom {
        display: flex;
        gap: 15px;
        margin-top: 40px;
    }

    .btn-update {
        background: var(--accent);
        color: #000;
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: 2px;
        padding: 18px 40px;
        border-radius: 12px;
        border: none;
        transition: 0.4s;
        flex: 2;
        cursor: pointer;
    }

    .btn-update:hover {
        background: #fff;
        transform: translateY(-3px);
        box-shadow: 0 10px 20px rgba(245, 158, 11, 0.3);
    }

    .btn-cancel {
        background: rgba(255, 255, 255, 0.05);
        color: #fff;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        padding: 18px 30px;
        border-radius: 12px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        text-decoration: none;
        text-align: center;
        transition: 0.3s;
        flex: 1;
        font-size: 0.8rem;
    }

    .btn-cancel:hover {
        background: rgba(220, 38, 38, 0.1);
        color: #ff4d4d;
        border-color: #ff4d4d;
    }
</style>

<div class="admin-form-wrapper">
    <div class="premium-form-card animate__animated animate__fadeIn">
        
        <div class="form-header">
            <h2>Sistem <span style="color: var(--accent)">Güncelleme</span></h2>
        </div>

        <form method="post" enctype="multipart/form-data">
            <input type="hidden" name="Id" value="@Model.Id" />

            <div class="form-group-custom mb-4">
                <label class="form-label">Makine Modeli / Adı</label>
                <input type="text" name="Name" value="@Model.Name" class="form-control-premium w-100" required />
            </div>

            <div class="form-group-custom mb-4">
                <label class="form-label">Teknik Detaylar</label>
                <textarea name="Description" class="form-control-premium w-100" rows="4">@Model.Description</textarea>
            </div>

            <div class="row">
                <div class="col-md-6">
                    <div class="form-group-custom mb-4">
                        <label class="form-label">Güncel Fiyat (₺)</label>
                        <input type="number" step="0.01" name="Price" value="@Model.Price" class="form-control-premium w-100" />
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group-custom mb-4">
                        <label class="form-label">Üretici Marka</label>
                        <select name="Brand" class="form-control-premium w-100">
                            <option value="@Model.Brand">@Model.Brand (Mevcut)</option>
                            <hr />
                            <option value="XCMG">XCMG</option>
                            <option value="Hyundai">Hyundai</option>
                            <option value="Hitachi">Hitachi</option>
                            <option value="Volvo">Volvo</option>
                            <option value="Komatsu">Komatsu</option>
                        </select>
                    </div>
                </div>
            </div>

            <div class="form-group-custom mb-4">
                <label class="form-label">Görseli Güncelle (Opsiyonel)</label>
                <input type="file" name="ImageFile" class="form-control-premium w-100" />
                <small style="color: rgba(255,255,255,0.3); font-size: 0.7rem; margin-top: 5px; display: block;">
                    Değiştirmek istemiyorsanız boş bırakın.
                </small>
            </div>

            <div class="btn-group-custom">
                <button type="submit" class="btn-update">Değişiklikleri Kaydet</button>
                <a href="/yonetim/panel" class="btn-cancel d-flex align-items-center justify-content-center">İptal Et</a>
            </div>

        </form>

    </div>
</div>